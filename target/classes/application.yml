spring:
  application:
    name: pulseapi
  profiles:
    active: dev

server:
  port: ${APP_PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:CHANGE_ME_DEV_SECRET_32_CHARS_MIN}
    expiration-minutes: ${JWT_EXP_MINUTES:120}
